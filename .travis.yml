language: java
install: true


dist: trusty
jdk:
  - openjdk13


branches:
  only:
  - master
  - v7.6.0
    

script:
  - ./gradlew test --tests 'org.elasticsearch.modules.reindex.*'
  - ./gradlew test --tests 'org.elasticsearch.modules.percolator.*'
  - ./gradlew test --tests 'org.elasticsearch.modules.ingest-geoip.*'
  - ./gradlew test --tests 'org.elasticsearch.modules.rank-eval.*'